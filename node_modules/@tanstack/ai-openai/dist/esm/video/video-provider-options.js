function validateVideoSize(model, size) {
  const validSizes = [
    "1280x720",
    "720x1280",
    "1792x1024",
    "1024x1792"
  ];
  if (size && !validSizes.includes(size)) {
    throw new Error(
      `Size "${size}" is not supported by model "${model}". Supported sizes: ${validSizes.join(", ")}`
    );
  }
}
function validateVideoSeconds(model, seconds) {
  const validSeconds = ["4", "8", "12"];
  const validNumbers = [4, 8, 12];
  if (seconds !== void 0) {
    const isValid = typeof seconds === "string" ? validSeconds.includes(seconds) : validNumbers.includes(seconds);
    if (!isValid) {
      throw new Error(
        `Duration "${seconds}" is not supported by model "${model}". Supported durations: 4, 8, or 12 seconds`
      );
    }
  }
}
function toApiSeconds(seconds) {
  if (seconds === void 0) return void 0;
  return String(seconds);
}
export {
  toApiSeconds,
  validateVideoSeconds,
  validateVideoSize
};
//# sourceMappingURL=video-provider-options.js.map
