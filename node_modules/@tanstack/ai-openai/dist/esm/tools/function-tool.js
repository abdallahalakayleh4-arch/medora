import { makeOpenAIStructuredOutputCompatible } from "../utils/schema-converter.js";
function convertFunctionToolToAdapterFormat(tool) {
  const inputSchema = tool.inputSchema ?? {
    type: "object",
    properties: {},
    required: []
  };
  const jsonSchema = makeOpenAIStructuredOutputCompatible(
    inputSchema,
    inputSchema.required || []
  );
  jsonSchema.additionalProperties = false;
  return {
    type: "function",
    name: tool.name,
    description: tool.description,
    parameters: jsonSchema,
    strict: true
    // Always use strict mode since our schema converter handles the requirements
  };
}
export {
  convertFunctionToolToAdapterFormat
};
//# sourceMappingURL=function-tool.js.map
